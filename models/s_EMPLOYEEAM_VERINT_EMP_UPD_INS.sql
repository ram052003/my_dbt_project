{{
    config(
        materialized='incremental',
        alias='TEMP_DIR+VERINT_EMP_UPD_INS',
        schema='',
        pre_hook=[],
        post_hook=[],
        incremental_strategy='overwrite'
    )
}}

WITH CURRENT_TIMESTAMPOut AS (
    SELECT
        TO_VARCHAR(CONVERT_TIMEZONE('UTC', CURRENT_TIMESTAMP()), 'YYYY-MM-DD HH24:MI:SS.FF3') AS UPDT_END_DTTM,
        TO_VARCHAR(DATEADD(SS, 1, CONVERT_TIMEZONE('UTC', CURRENT_TIMESTAMP())), 'YYYY-MM-DD HH24:MI:SS.FF3') AS EFF_DTTM,
        TO_VARCHAR(CURRENT_TIMESTAMP(), 'YYYY-MM-DD HH24:MI:SS.FF3') AS AUD_CRE_DTTM
),

EMPLOYEEAMOut AS (
    SELECT
        a.ID AS EMP_ID,
        a.EMPLOYEETYPEID AS EMP_TYPE_ID,
        a.ASSIGNEDPOINTS AS ASGN_PNT,
        a.EMPLOYEENUMBER AS EMP_NBR,
        a.STARTTIME AS EMP_STRT_DTTM,
        a.ENDTIME AS EMP_END_DTTM,
        a.CHANGECOUNTER AS CHG_CNTR,
        a.MODIFIEDBY AS MOD_BY,
        a.ISSUPERVISOR AS SPVSR_FLG,
        a.ISTEAMLEAD AS TEAM_LEAD_FLG,
        a.PREFERREDSTART AS PREF_STRT_FLG,
        b.FIRSTNAME AS FRST_NM,
        b.LASTNAME AS LAST_NM,
        b.MIDDLEINITIAL AS MDL_INITL,
        b.SUFFIX AS SUFX,
        b.BIRTHDATE AS BRTH_DT,
        c.USERNAME AS USR_NM,
        c.STATUS AS USR_STS,
        c.MODIFIEDBY AS PROFILE_MOD_BY,
        c.LASTMODIFIEDAT AS PROFILE_MOD_DTTM,
        c.FAILEDLOGINCOUNT AS FAIL_LOGIN_CNT,
        c.LASTLOGINTIME AS LAST_LOGIN_DTTM
    FROM {{ ref('dbo.EMPLOYEEAM') }} AS a
    JOIN {{ ref('dbo.PERSON') }} AS b
        ON a.PERSONID = b.ID
    LEFT JOIN {{ ref('dbo.BPUSER') }} AS c
        ON a.ID = c.EMPLOYEEID
),

VERINT_EMPOut AS (
    SELECT
        EMP_ID,
        SOR_CD,
        EFF_DTTM,
        END_DTTM,
        UNQ_KEY_TXT,
        EMP_TYPE_ID,
        ASGN_PNT,
        EMP_NBR,
        EMP_STRT_DTTM,
        EMP_END_DTTM,
        CHG_CNTR,
        MOD_BY,
        SPVSR_FLG,
        TEAM_LEAD_FLG,
        PREF_STRT_FLG,
        FRST_NM,
        LAST_NM,
        MDL_INITL,
        SUFX,
        BRTH_DT,
        USR_NM,
        USR_STS,
        PROFILE_MOD_BY,
        PROFILE_MOD_DTTM,
        FAIL_LOGIN_CNT,
        LAST_LOGIN_DTTM
    FROM {{ ref('dbo.VERINT_EMP') }}
    WHERE CURR_IND = 'Y'
),

LKP_CRCOut AS (
    SELECT
        EMP_ID,
        SOR_CD,
        EFF_DTTM,
        END_DTTM,
        UNQ_KEY_TXT,
        EMP_TYPE_ID,
        ASGN_PNT,
        EMP_NBR,
        EMP_STRT_DTTM,
        EMP_END_DTTM,
        CHG_CNTR,
        MOD_BY,
        SPVSR_FLG,
        TEAM_LEAD_FLG,
        PREF_STRT_FLG,
        FRST_NM,
        LAST_NM,
        MDL_INITL,
        SUFX,
        BRTH_DT,
        USR_NM,
        USR_STS,
        PROFILE_MOD_BY,
        PROFILE_MOD_DTTM,
        FAIL_LOGIN_CNT,
        LAST_LOGIN_DTTM,
        MD5(
            CONCAT(
                COALESCE(EMP_TYPE_ID::VARCHAR, ''), '|',
                COALESCE(ASGN_PNT::VARCHAR, ''), '|',
                COALESCE(EMP_NBR::VARCHAR, ''), '|',
                COALESCE(EMP_STRT_DTTM::VARCHAR, ''), '|',
                COALESCE(EMP_END_DTTM::VARCHAR, ''), '|',
                COALESCE(CHG_CNTR::VARCHAR, ''), '|',
                COALESCE(MOD_BY::VARCHAR, ''), '|',
                COALESCE(SPVSR_FLG::VARCHAR, ''), '|',
                COALESCE(TEAM_LEAD_FLG::VARCHAR, ''), '|',
                COALESCE(PREF_STRT_FLG::VARCHAR, ''), '|',
                COALESCE(FRST_NM::VARCHAR, ''), '|',
                COALESCE(LAST_NM::VARCHAR, ''), '|',
                COALESCE(MDL_INITL::VARCHAR, ''), '|',
                COALESCE(SUFX::VARCHAR, ''), '|',
                COALESCE(BRTH_DT::VARCHAR, ''), '|',
                COALESCE(USR_NM::VARCHAR, ''), '|',
                COALESCE(USR_STS::VARCHAR, ''), '|',
                COALESCE(PROFILE_MOD_BY::VARCHAR, ''), '|',
                COALESCE(PROFILE_MOD_DTTM::VARCHAR, ''), '|',
                COALESCE(FAIL_LOGIN_CNT::VARCHAR, ''), '|',
                COALESCE(LAST_LOGIN_DTTM::VARCHAR, '')
            )
        ) AS CRC
    FROM VERINT_EMPOut
),

tAddCRCRow_3_Lookup_LastMatchOut AS (
    SELECT DISTINCT
        EMP_ID,
        LAST_VALUE(SOR_CD) OVER (PARTITION BY EMP_ID ORDER BY EMP_ID) AS SOR_CD,
        LAST_VALUE(EFF_DTTM) OVER (PARTITION BY EMP_ID ORDER BY EMP_ID) AS EFF_DTTM,
        LAST_VALUE(END_DTTM) OVER (PARTITION BY EMP_ID ORDER BY EMP_ID) AS END_DTTM,
        LAST_VALUE(UNQ_KEY_TXT) OVER (PARTITION BY EMP_ID ORDER BY EMP_ID) AS UNQ_KEY_TXT,
        LAST_VALUE(EMP_TYPE_ID) OVER (PARTITION BY EMP_ID ORDER BY EMP_ID) AS EMP_TYPE_ID,
        LAST_VALUE(ASGN_PNT) OVER (PARTITION BY EMP_ID ORDER BY EMP_ID) AS ASGN_PNT,
        LAST_VALUE(EMP_NBR) OVER (PARTITION BY EMP_ID ORDER BY EMP_ID) AS EMP_NBR,
        LAST_VALUE(EMP_STRT_DTTM) OVER (PARTITION BY EMP_ID ORDER BY EMP_ID) AS EMP_STRT_DTTM,
        LAST_VALUE(EMP_END_DTTM) OVER (PARTITION BY EMP_ID ORDER BY EMP_ID) AS EMP_END_DTTM,
        LAST_VALUE(CHG_CNTR) OVER (PARTITION BY EMP_ID ORDER BY EMP_ID) AS CHG_CNTR,
        LAST_VALUE(MOD_BY) OVER (PARTITION BY EMP_ID ORDER BY EMP_ID) AS MOD_BY,
        LAST_VALUE(SPVSR_FLG) OVER (PARTITION BY EMP_ID ORDER BY EMP_ID) AS SPVSR_FLG,
        LAST_VALUE(TEAM_LEAD_FLG) OVER (PARTITION BY EMP_ID ORDER BY EMP_ID) AS TEAM_LEAD_FLG,
        LAST_VALUE(PREF_STRT_FLG) OVER (PARTITION BY EMP_ID ORDER BY EMP_ID) AS PREF_STRT_FLG,
        LAST_VALUE(FRST_NM) OVER (PARTITION BY EMP_ID ORDER BY EMP_ID) AS FRST_NM,
        LAST_VALUE(LAST_NM) OVER (PARTITION BY EMP_ID ORDER BY EMP_ID) AS LAST_NM,
        LAST_VALUE(MDL_INITL) OVER (PARTITION BY EMP_ID ORDER BY EMP_ID) AS MDL_INITL,
        LAST_VALUE(SUFX) OVER (PARTITION BY EMP_ID ORDER BY EMP_ID) AS SUFX,
        LAST_VALUE(BRTH_DT) OVER (PARTITION BY EMP_ID ORDER BY EMP_ID) AS BRTH_DT,
        LAST_VALUE(USR_NM) OVER (PARTITION BY EMP_ID ORDER BY EMP_ID) AS USR_NM,
        LAST_VALUE(USR_STS) OVER (PARTITION BY EMP_ID ORDER BY EMP_ID) AS USR_STS,
        LAST_VALUE(PROFILE_MOD_BY) OVER (PARTITION BY EMP_ID ORDER BY EMP_ID) AS PROFILE_MOD_BY,
        LAST_VALUE(PROFILE_MOD_DTTM) OVER (PARTITION BY EMP_ID ORDER BY EMP_ID) AS PROFILE_MOD_DTTM,
        LAST_VALUE(FAIL_LOGIN_CNT) OVER (PARTITION BY EMP_ID ORDER BY EMP_ID) AS FAIL_LOGIN_CNT,
        LAST_VALUE(LAST_LOGIN_DTTM) OVER (PARTITION BY EMP_ID ORDER BY EMP_ID) AS LAST_LOGIN_DTTM,
        LAST_VALUE(CRC) OVER (PARTITION BY EMP_ID ORDER BY EMP_ID) AS CRC
    FROM LKP_CRCOut
),

SRC_CRCOut AS (
    SELECT
        EMP_ID,
        EMP_TYPE_ID,
        ASGN_PNT,
        EMP_NBR,
        EMP_STRT_DTTM,
        EMP_END_DTTM,
        CHG_CNTR,
        MOD_BY,
        SPVSR_FLG,
        TEAM_LEAD_FLG,
        PREF_STRT_FLG,
        FRST_NM,
        LAST_NM,
        MDL_INITL,
        SUFX,
        BRTH_DT,
        USR_NM,
        USR_STS,
        PROFILE_MOD_BY,
        PROFILE_MOD_DTTM,
        FAIL_LOGIN_CNT,
        LAST_LOGIN_DTTM,
        MD5(
            CONCAT(
                COALESCE(EMP_TYPE_ID::VARCHAR, ''), '|',
                COALESCE(ASGN_PNT::VARCHAR, ''), '|',
                COALESCE(EMP_NBR::VARCHAR, ''), '|',
                COALESCE(EMP_STRT_DTTM::VARCHAR, ''), '|',
                COALESCE(EMP_END_DTTM::VARCHAR, ''), '|',
                COALESCE(CHG_CNTR::VARCHAR, ''), '|',
                COALESCE(MOD_BY::VARCHAR, ''), '|',
                COALESCE(SPVSR_FLG::VARCHAR, ''), '|',
                COALESCE(TEAM_LEAD_FLG::VARCHAR, ''), '|',
                COALESCE(PREF_STRT_FLG::VARCHAR, ''), '|',
                COALESCE(FRST_NM::VARCHAR, ''), '|',
                COALESCE(LAST_NM::VARCHAR, ''), '|',
                COALESCE(MDL_INITL::VARCHAR, ''), '|',
                COALESCE(SUFX::VARCHAR, ''), '|',
                COALESCE(BRTH_DT::VARCHAR, ''), '|',
                COALESCE(USR_NM::VARCHAR, ''), '|',
                COALESCE(USR_STS::VARCHAR, ''), '|',
                COALESCE(PROFILE_MOD_BY::VARCHAR, ''), '|',
                COALESCE(PROFILE_MOD_DTTM::VARCHAR, ''), '|',
                COALESCE(FAIL_LOGIN_CNT::VARCHAR, ''), '|',
                COALESCE(LAST_LOGIN_DTTM::VARCHAR, '')
            )
        ) AS CRC
    FROM EMPLOYEEAMOut
),

tMap_1Out AS (
    SELECT
        SRC_CRCOut.EMP_NBR AS EMP_NBR,
        SRC_CRCOut.ASGN_PNT AS ASGN_PNT,
        SRC_CRCOut.USR_STS AS USR_STS,
        SRC_CRCOut.EMP_TYPE_ID AS EMP_TYPE_ID,
        SRC_CRCOut.MOD_BY AS MOD_BY,
        SRC_CRCOut.FRST_NM AS FRST_NM,
        SRC_CRCOut.SPVSR_FLG AS SPVSR_FLG,
        SRC_CRCOut.SUFX AS SUFX,
        tAddCRCRow_3_Lookup_LastMatchOut.EMP_ID AS EMP_ID_2,
        SRC_CRCOut.EMP_ID AS EMP_ID_1,
        SRC_CRCOut.MDL_INITL AS MDL_INITL,
        SRC_CRCOut.LAST_LOGIN_DTTM AS LAST_LOGIN_DTTM,
        SRC_CRCOut.FAIL_LOGIN_CNT AS FAIL_LOGIN_CNT,
        CURRENT_TIMESTAMPOut.UPDT_END_DTTM AS UPDT_END_DTTM,
        CURRENT_TIMESTAMPOut.EFF_DTTM AS EFF_DTTM_1,
        SRC_CRCOut.BRTH_DT AS BRTH_DT,
        tAddCRCRow_3_Lookup_LastMatchOut.EFF_DTTM AS EFF_DTTM_2,
        SRC_CRCOut.TEAM_LEAD_FLG AS TEAM_LEAD_FLG,
        SRC_CRCOut.PROFILE_MOD_BY AS PROFILE_MOD_BY,
        SRC_CRCOut.USR_NM AS USR_NM,
        SRC_CRCOut.CRC AS CRC_1,
        tAddCRCRow_3_Lookup_LastMatchOut.CRC AS CRC_2,
        SRC_CRCOut.EMP_STRT_DTTM AS EMP_STRT_DTTM,
        SRC_CRCOut.LAST_NM AS LAST_NM,
        SRC_CRCOut.PROFILE_MOD_DTTM AS PROFILE_MOD_DTTM,
        SRC_CRCOut.CHG_CNTR AS CHG_CNTR,
        CURRENT_TIMESTAMPOut.AUD_CRE_DTTM AS AUD_CRE_DTTM,
        SRC_CRCOut.EMP_END_DTTM AS EMP_END_DTTM,
        SRC_CRCOut.PREF_STRT_FLG AS PREF_STRT_FLG
    FROM SRC_CRCOut
    CROSS JOIN CURRENT_TIMESTAMPOut
    INNER JOIN tAddCRCRow_3_Lookup_LastMatchOut
        ON SRC_CRCOut.EMP_ID = tAddCRCRow_3_Lookup_LastMatchOut.EMP_ID
),

Router_tMap_1Out2 AS (
    SELECT
        EMP_ID_1 AS EMP_ID,
        EMP_TYPE_ID,
        ASGN_PNT,
        EMP_NBR,
        EMP_STRT_DTTM,
        EMP_END_DTTM,
        CHG_CNTR,
        MOD_BY,
        SPVSR_FLG,
        TEAM_LEAD_FLG,
        PREF_STRT_FLG,
        FRST_NM,
        LAST_NM,
        MDL_INITL,
        SUFX,
        BRTH_DT,
        USR_NM,
        USR_STS,
        PROFILE_MOD_BY,
        PROFILE_MOD_DTTM,
        FAIL_LOGIN_CNT,
        LAST_LOGIN_DTTM,
        CRC_1,
        UPDT_END_DTTM,
        EFF_DTTM_1,
        AUD_CRE_DTTM,
        EMP_ID_2,
        SOR_CD,
        EFF_DTTM_2,
        END_DTTM,
        UNQ_KEY_TXT,
        EMP_TYPE_ID AS REF_EMP_TYPE_ID,
        ASGN_PNT AS REF_ASGN_PNT,
        EMP_NBR AS REF_EMP_NBR,
        EMP_STRT_DTTM AS REF_EMP_STRT_DTTM,
        EMP_END_DTTM AS REF_EMP_END_DTTM,
        CHG_CNTR AS REF_CHG_CNTR,
        MOD_BY AS REF_MOD_BY,
        SPVSR_FLG AS REF_SPVSR_FLG,
        TEAM_LEAD_FLG AS REF_TEAM_LEAD_FLG,
        PREF_STRT_FLG AS REF_PREF_STRT_FLG,
        FRST_NM AS REF_FRST_NM,
        LAST_NM AS REF_LAST_NM,
        MDL_INITL AS REF_MDL_INITL,
        SUFX AS REF_SUFX,
        BRTH_DT AS REF_BRTH_DT,
        USR_NM AS REF_USR_NM,
        USR_STS AS REF_USR_STS,
        PROFILE_MOD_BY AS REF_PROFILE_MOD_BY,
        PROFILE_MOD_DTTM AS REF_PROFILE_MOD_DTTM,
        FAIL_LOGIN_CNT AS REF_FAIL_LOGIN_CNT,
        LAST_LOGIN_DTTM AS REF_LAST_LOGIN_DTTM,
        CRC_2 AS REF_CRC
    FROM tMap_1Out
    WHERE CRC_1 <> CRC_2
),

lnk_upd_insOut AS (
    SELECT
        EMP_ID,
        'VERINT' AS SOR_CD,
        EFF_DTTM,
        TO_TIMESTAMP_NTZ('9999-12-31 23:59:59.9999999', 'yyyy-MM-dd HH:mm:ss.SSS') AS END_DTTM,
        EMP_ID || '~' || 'VERINT' AS UNQ_KEY_TXT,
        EMP_TYPE_ID,
        ASGN_PNT,
        writeSplForNullString(EMP_NBR) AS EMP_NBR,
        EMP_STRT_DTTM,
        EMP_END_DTTM,
        CHG_CNTR,
        writeSplForNullString(MOD_BY) AS MOD_BY,
        writeSplForNullString(SPVSR_FLG) AS SPVSR_FLG,
        writeSplForNullString(TEAM_LEAD_FLG) AS TEAM_LEAD_FLG,
        writeSplForNullString(PREF_STRT_FLG) AS PREF_STRT_FLG,
        writeSplForNullString(FRST_NM) AS FRST_NM,
        writeSplForNullString(LAST_NM) AS LAST_NM,
        writeSplForNullString(MDL_INITL) AS MDL_INITL,
        writeSplForNullString(SUFX) AS SUFX,
        BRTH_DT,
        writeSplForNullString(USR_NM) AS USR_NM,
        USR_STS,
        writeSplForNullString(PROFILE_MOD_BY) AS PROFILE_MOD_BY,
        PROFILE_MOD_DTTM,
        FAIL_LOGIN_CNT,
        LAST_LOGIN_DTTM,
        'ETL-INSERT' AS AUD_CRE_BY_NM,
        AUD_CRE_DTTM,
        'Y' AS CURR_IND,
        ETL_BATCH_ID
    FROM tMap_1Out
    WHERE CRC_1 <> CRC_2
)

SELECT
    EMP_ID,
    SOR_CD,
    EFF_DTTM,
    END_DTTM,
    UNQ_KEY_TXT,
    EMP_TYPE_ID,
    ASGN_PNT,
    EMP_NBR,
    EMP_STRT_DTTM,
    EMP_END_DTTM,
    CHG_CNTR,
    MOD_BY,
    SPVSR_FLG,
    TEAM_LEAD_FLG,
    PREF_STRT_FLG,
    FRST_NM,
    LAST_NM,
    MDL_INITL,
    SUFX,
    BRTH_DT,
    USR_NM,
    USR_STS,
    PROFILE_MOD_BY,
    PROFILE_MOD_DTTM,
    FAIL_LOGIN_CNT,
    LAST_LOGIN_DTTM,
    AUD_CRE_BY_NM,
    AUD_CRE_DTTM,
    CURR_IND,
    ETL_BATCH_ID
FROM lnk_upd_insOut