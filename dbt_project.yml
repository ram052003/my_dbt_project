name: my_dbt_project
version: 1.0
config-version: 2

profile: my_dbt_project

model-paths: ["models"]

models:
  my_dbt_project:
    staging:
      +schema: analytics
      +materialized: view

    marts:
      +schema: analytics
      +materialized: table
